apiVersion: v1
kind: Service
metadata:
  name: incap-frontend
spec:
  type: NodePort
  ports:
  - name: "8080"
    port: 8080
    targetPort: 8080
    nodePort: 30080
  selector:
    app: incap-frontend
status:
  loadBalancer: {}
